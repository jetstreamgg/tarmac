/** Matches the dismissal in `components/ui/dialog.tsx` and the bottom sheet's, both 300ms. */
export const MODAL_EXIT_MS = 300;

export enum AnimationLabels {
  initial = 'initial',
  animate = 'animate',
  exit = 'exit'
}
