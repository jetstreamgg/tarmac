TENDERLY_API_KEY=YourApiKeyToken

# Tenderly mainnet parent vnet for `pnpm vnet:fork` (POST /vnets/fork vnet_id).
# Must be a forkable parent with PT-sUSDS deployed (block ~25452992).
# Dashboard "testnet" slugs are not valid fork parents — use a vnet id from the API.
MAINNET_FORK_CONTAINER_ID=28c53f60-d0a2-4cdc-8245-f22019e9c139